<div class="jumbotron text-center">
    <h1>Welcome to Arun Match</h1>
    <h3>A membership site where enterpreneurs can meet developers</h3>
</div>

<div class="row">
    <% if user_signed_in? %>
        <div class="col-md-6">
            <div class="well">
                <h2 class="text-center">Your Profile</h2>
                <% if current_user.profile %>
                    <%= link_to "Edit your Profile", edit_user_profile_path(current_user), class: "btn btn-default btn-lg btn-block" %>
                    <%= link_to "View your Profile", user_path(current_user), class: "btn btn-default btn-lg btn-block" %>
                <% else %>
                    <p>Create your profile so that you can share your information with the community.</p>
                    <%= link_to "Create your Profile!", new_user_profile_path(current_user), class: "btn btn-default btn-lg btn-block" %>
                <% end %>
            </div>
        </div>
    <div class="col-md-6">
        <div class="well">
            <h2 class="text-center">The community</h2>
            <p>Visit the community to meet other Developers, Entrepreneurs, Investor</p>
            <%= link_to "Visit the community", "#", class: "btn btn-default btn-lg btn-block" %>
        </div>
    </div>    
    <% else %>
        <div class="col-md-6">
            <div class="well">
                <h2 class="text-center">Basic Membership</h2>
                <h4>Sign up for free and get basic access to our community.</h4>
                <br/>
                <%= link_to 'Sign Up Basic', new_user_registration_path(plan: @basic_plan), class: 'btn btn-success btn-lg btn-block'%>
            </div>
        </div>
        
        <div class="col-md-6">
            <div class="well">
                <h2 class="text-center">Pro Membership</h2>
                <h4>Sign up for the pro account for $10/month and get access to the community and contact info!</h4>
                <%= link_to 'Sign Up Pro', new_user_registration_path(plan: @pro_plan), class: 'btn btn-success btn-lg btn-block'%>
            </div>
        </div>
    <% end %>
</div>